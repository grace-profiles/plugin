description: Adds Asset Pipeline to a Grace Plugin for packaging
build:
    plugins:
        - asset-pipeline
dependencies:
    - scope: build
      coords: 'org.graceframework.plugins:asset-pipeline-gradle:5.0.1'
    - scope: runtime
      coords: "org.graceframework.plugins:asset-pipeline-plugin:5.0.1"
