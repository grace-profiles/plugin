description: Adds Asset Pipeline to a Grails Plugin for packaging
build:
    plugins:
        - asset-pipeline
dependencies:
    build:
        - 'com.bertramlabs.plugins:asset-pipeline-gradle:3.0.10'
    runtime:
        - "com.bertramlabs.plugins:asset-pipeline-grails:3.0.10"
